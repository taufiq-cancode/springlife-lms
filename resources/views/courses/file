<div class="col-lg-8">
    <h4 class="mb-1">Lesson Title</h4>
    <iframe width="560" height="315" src="lesson_link" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe>
</div>

<div class="col-lg-4">   
    @foreach ($lessons as $lesson)
        <input class="form-check-input" type="checkbox" id="lesson_{{ $lesson->id }}" checked="">
        
        <label for="lesson_{{ $lesson->id }}">
            <span>{{ $loop->iteration }}. {{ $lesson->title }}</span>
            <span> min</span>
        </label>
    @endforeach
</div>
